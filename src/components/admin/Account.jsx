import React from 'react';
import PropTypes from 'prop-types';

const Account = props => {
    return (
        <div className='account'>
            <div className='avt'>
            </div>
            <p>Admin</p>
        </div>
    );
};

Account.propTypes = {
    
};

export default Account;